import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    common: {
      black: "#000",
      white: "#fff",
    },
    primary: {
      main: "#5d4037",
      light: "#8d6e63",
      dark: "#4e342e",
    },
    secondary: {
      main: "#ffa000",
      light: "#ffa000",
      dark: "#ff8f00",
    },
  },
});
